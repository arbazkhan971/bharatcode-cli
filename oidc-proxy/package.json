{
  "name": "oidc-proxy",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "^3.0.0",
    "@cloudflare/vitest-pool-workers": "^0.18.4"
  }
}
